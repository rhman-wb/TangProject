# 配图策略：占位符 + Grok 生图批量替换

v1 的地点/诗词配图先用统一占位符（每图附 `prompt` 与 `status: placeholder`）上线，后续用 **Grok 自带生图模型**按统一的唐风美术方向（水墨/工笔/古画质感，与 Design §5.2 色板呼应）批量生成、替换占位并置 `status: generated`。这样彻底规避配图版权风险——不依赖公有领域/CC 来源的逐一登记与检索；代价是需要先在设计阶段定出 art-direction prompt 模板。
