{
  "name": "chang-an-poetry-map",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "唐诗地图（长安诗境）— 纯前端静态站点",
  "scripts": {
    "validate": "node scripts/validate-data.mjs",
    "test": "node --test tests/**/*.test.js",
    "serve": "npx --yes serve -l 5173 .",
    "dev": "npx --yes serve -l 5173 ."
  },
  "engines": {
    "node": ">=18"
  }
}
